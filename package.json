{
  "scripts": {
    "dev": "vite",
    "build": "vite build && cp -r assets audio dist/",
    "preview": "vite preview"
  },
  "dependencies": {
    "phaser": "^4.2.1",
    "phaser4-rex-plugins": "^4.2.0",
    "unmute-ios-audio": "^3.3.0"
  },
  "devDependencies": {
    "esbuild": "^0.28.2",
    "vite": "^6.3.5"
  }
}
